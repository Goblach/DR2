POL_vnesnyaya_politika = {
	
	DR_POL_LUB_war = {
	
		cost = 25

		available = {
				tag = POL
			date > 5.02.01
			has_war = no 
			is_neighbor_of = LUB
			NOT = { country_exists = KLG }
			LUB = { is_subject = no } 
		}
		
		visible = {
			country_exists = LUB
		}
		
		fire_only_once = yes
		
		days_remove = 7
		
		ai_will_do = {
			base = 10.5
		}
		
		remove_effect = {
			create_wargoal = {
				target = LUB
				type = annex_everything
            }
		}			
	}
	
}